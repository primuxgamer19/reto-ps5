# ui_config.py
from kivy.metrics import dp

# Padding usado por render y control táctil (centralizado para evitar desajustes)
DEFAULT_PADDING = dp(55)

# Ventana por defecto en días para el enfoque en lo más reciente
DEFAULT_VIEW_SPAN_DAYS = 30

# Padding relativo para eje Y al centrar la vista (10% por defecto)
DEFAULT_VIEW_Y_PADDING = 0.50